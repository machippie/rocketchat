# Standards: 0.1
---
# @var rocketchat_user:description: User to run user-specific commands
# @var rocketchat_user:value: rocketchat_user | default(homebrew_user) | default(ansible_user_id)
# rocketchat_user:

# @var rocketchat_started:description: Restart app if already running
rocketchat_started: True

...
